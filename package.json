{
  "name": "engineerya-root",
  "private": true,
  "description": "Root package — semua source ada di subfolder engineerya/. Jalankan perintah dari sana.",
  "scripts": {
    "install:all": "npm install --prefix engineerya",
    "dev": "npm run dev --prefix engineerya",
    "build": "npm run build --prefix engineerya",
    "lint": "npm run lint --prefix engineerya",
    "typecheck": "npm run typecheck --prefix engineerya",
    "test": "npm run test --prefix engineerya"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
